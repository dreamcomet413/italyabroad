  <script type="text/javascript">
    $(document).ready(function(){

			var start_price =   <%= params[:start_price] and !params[:start_price].blank? ? params[:start_price] : 0%>;
		    var end_price   =   <%= params[:end_price] and !params[:end_price].blank? ? params[:end_price] : 150 %>;
		    $( "#amount" ).val( "£" + start_price + " - £" + end_price );	 
		    $("#slider-range").slider('values',[start_price, end_price]);


	    $('#mood').val('<%= params[:mood]%>');
	    $('#region_id').val('<%= params[:region_id]%>');
	    $('#grape_id').val('<%= params[:grape_id]%>');
	    $('#producer_id').val('<%= params[:producer_id]%>');
	    $('#grape_id').val('<%= params[:grape_id]%>');
	    $('#organic').val('<%= params[:organic]%>');
	    $('#vegetarian').val('<%= params[:vegetarian]%>');


    });
  </script>
