<div class="block" id="box_most_read_recipes">
  <h1>Most read recipes</h1>
  <ul>
    <%- Recipe.active.most_viewed.paginate(:page => params[:page], :per_page => 3).each do |recipe| -%>
    <li><%= link_to recipe.name, recipe_path(recipe) %></li>
    <%- end -%>
  </ul>
</div>