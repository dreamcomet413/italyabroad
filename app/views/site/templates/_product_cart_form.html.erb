<%= form_tag("/site/cart?product_id=#{product.id}" , :class=>"cart_form", method: "post" , :remote=>true) do %>
	<%=hidden_field_tag :product_id , product.id , :class=>'p_id'  %>
	<%#=hidden_field_tag :p_id , product.id , :class=>'p_id'  %>
	
	<div class="seller-field"><%= text_field_tag(:quantity , 1 , :class=>'quantity') %></div>
	<div class="seller-btn">
		<%= link_to "Add", "#.", :class=>'submit_cart_form'%>
	</div>
<%end%>
