<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <title><%= h(title) %></title>
  <%= csrf_meta_tag %>
  <link rel="shortcut icon" href="/images/italyabroad.ico" />
  <link rel="alternate" type="application/rss+xml" title="Italyabroad.com Products, Feed RSS 2.0" href="<%= products_url %>" />
  <link rel="alternate" type="application/rss+xml" title="Italyabroad.com Blog, Feed RSS 2.0" href="<%= site_blog_index_url %>" />
  <meta name="description" content="<%= h(description) %>" />
  <meta name="keywords" content="<%= h(keys) %>" />
  <meta name="copyright" content="italyabroad.com" />
  <meta http-equiv="content-language" content="EN" />
  <meta http-equiv="content-type" content="text/html;charset=utf-8" />
  <meta name="ROBOTS" content="INDEX,FOLLOW" />
  <meta name="creation_Date" content="09/21/2007" />
  <meta name="revisit-after" content="7 days" />
  <meta name="verify-v1" content="4MIDVULSRTLwLBnaUwWYWG+BeU+y0EpGRk33kFSVSYo=" />
  <meta name="msvalidate.01" content="18C4C4F5FBD1413AA93AEAD1738CD7F5" />
  <meta name="blogcatalog" content="9BC8939820" />
  <%= stylesheet_link_tag "site", "menu", "lightbox", "omniauth" %>
  <%= javascript_include_tag :defaults, "slide", "lightbox-2.6.min" %>
  <!--[if gte IE 5.5]>
    <%= javascript_include_tag "menu" %>
	<![endif]-->

  <%= stylesheet_link_tag "default" ,"alphacube"%>
  <%#= javascript_include_tag  "window" %>


  <!-- Place this tag in your head or just before your close body tag -->
  <script type="text/javascript" src="https://apis.google.com/js/plusone.js"></script>

  <!-- JAVASCRIPT BLINK START -->


  <SCRIPT>
      // Before you reuse this script you may want to have your head examined
      //
      // Copyright 1999 InsideDHTML.com, LLC.

      function doBlink() {
          // Blink, Blink, Blink...
          var blink = document.all.tags("blink")
          for (var i=0; i < blink.length; i++)
              blink[i].style.visibility = blink[i].style.visibility == "" ? "hidden" : ""
      }

      function startBlink() {
          // Make sure it is IE4
          if (document.all)
              setInterval("doBlink()",1000)
      }
      window.onload = startBlink;
  </SCRIPT>

  <!-- Javascript Blink End-->


</head>
<body <%= @paypal_redirect %> <%= "id='#{@page_id}'" if @page_id %> class="<%= @current_controller %><%= " #{@class}" if @class %>" <%= "onload='#{@onload}'" if @onload %>>
<div id="content">
<div id="topContent">
  <%#= link_to image_tag('logo.png', :alt => "Italian wine and food", :title => "Italian wine and food"), root_path, :id => "logo" %>
  <%= link_to image_tag('logo_hi_res.gif', :alt => "Italian wine and food", :title => "Italian wine and food"), root_path, :id => "logo" %>
  <div id="cartLogin">

    <div id="googleplus_lg">
      <!--&lt;!&ndash; Place this tag where you want the +1 button to render &ndash;&gt;<g:plusone></g:plusone>-->
    </div>
    <table id="cart" style="margin-top: -7px;">
      <tr>
        <td>
          <%= render :partial => "/site/shared/cart" %>
        </td>
        <td><%= link_to image_tag("/images/twitter.png", :height => "23px", :width => "23px"), "https://twitter.com/Italyabroad", :id => "b", :class => "btn" -%></td>
        <td><%= link_to image_tag("/images/facebook.gif", :height => "23px", :width => "23px"), "https://www.facebook.com/pages/Italyabroadcom/144011672282174" -%></td>
        <td><%= link_to image_tag("/images/googleplus.ico", :height => "23px", :width => "23px"), "https://plus.google.com/105030552023597415239/posts" -%></td>
      </tr>
    </table>

    <div id="logIn">
      <!-- Remove this line and uncomment the other lines when u go live -->
      <%# support_link = "Closed" -%>
      <% if !Setting.first.nil? %>
          <%  @support_user = Setting.find(:first).support
              #@support_status = Juggernaut.show_client(@support_user) ? true : false
              #   @support_status = (Juggernaut.show_client(@support_user) and @setting.chat_available == true) ? true : false
              @support_status = (@setting.chat_available == true) ? true : false
              #  @support_status = false
              if @support_status == true
                support_link = "available"
                image_name="live_chat_available.png"
              else
                support_link = "unavailable"
                image_name="live_chat_unavailable.png"
              end
          %>
      <%end%>
      <% if logged_in? %>
          Welcome <%= current_user.name %> <% if current_user.type_id == 1 %> | <%= link_to "Admin Site",siteadmin_path %><% end %> |

          <% if Message.find(:all,:conditions=>['user_id = ?',current_user.id]).count > 0 %>
              <%= link_to "My Account",  :controller=>'site/customers',:action=>'show',:id=>current_user.id  %> |
          <% else %>
              <%= link_to "My Account", account_path %> |
          <% end %>
          <%= link_to "Log out", logout_path %>
      <% else %>
          <%= link_to "Log in", login_path %> | <%= link_to "Sign up", signup_path %>
      <% end %>
        | <%= link_to "Contact Us", contact_us_path %>
    </div>
    <br /><div style="clear: both;"></div>
    <div style="margin-top: 12px;">

      <% if logged_in? %>

          <span style="font:bold 13px Arial;"><%#= link_to image_tag(image_name), site_chat_index_path, :popup => ['new_window_name', 'height=540,width=490'] if !image_name.nil?  %></span>
          <span style="font:bold 13px Arial; margin-left: 28px;" class="fLeft"><%= link_to image_tag(image_name), "#", :class => "chat_available" if !image_name.nil?  %></span>

      <% else %>
          <span style="font:bold 13px Arial; margin-left: 28px;" class="fLeft"><%= link_to(image_tag(image_name), site_chat_index_path, {:popup => ['new_window_name', 'height=540,width=490,resizable=yes,scrollbars=auto']}) if !image_name.nil? %></span>

      <% end %>





    <!--<div style="clear: both;"></div>-->
    <span id="search_form">
      <%- form_tag search_index_path, :method => :get do -%>
          <table>
            <tbody>
            <% if params[:text]
                 search_text = params["text"]
               else
                 search_text = 'Search Italyabroad.com'

               end %>
            <td>
              <div id="textbox-wrapper">
                <%= text_field_tag :text, search_text,:onclick=>"
                  if($('#text').val() == 'Search Italyabroad.com'){
                  $('#text').val('');}", :style=>"" %>
              </div>
            </td>
            <td>
              <div id="dropdown_wrapper">
                <%= select_tag :id, options_for_select([['Wine', 'wine'], ['Rose', 'rose'], ['Food', 'food'], ['Recipe', 'recipe'],['Hampers','hampers'], ['People', 'people'],['Chef', 'chef'],['All Site','all']], (params[:id] || params[:category])) %>
              </div>
            </td>
            <td>
              <%= image_submit_tag("searchButton.png", :name => nil, :alt => "search") %>
            </td>
            </tbody>
          </table>
      <%- end -%>
    </span>
    </div>


  </div>
  <div id="nav">
    <%- active_uri = request.request_uri -%>
    <ul class="nav1" style="z-index:1000;">  <!-- sujith modification z-index altered to try fixing the menu display issue -->
      <!--<li id="link-to-home" class="first<%#= " active" if active_uri == "/"%>"><%#= link_to "Home", root_path %></li>-->
      <% for category in ["Wine", "Other Drinks", "Food", "Hampers", "Wine Tours", "Wine Club", "Events"] do %>
          <% category = Category.find_by_name(category)%>

          <% if category.present? %>
              <li id="link-to-<%= category.friendly_identifier %>" class="<%= "active" if active_uri == "/#{category.friendly_identifier}"   %>">
                <%= link_to category.name, "/#{category.try(:friendly_identifier)}" %>

                <%- unless category.children.blank? -%>
                    <ul>
                      <% wine_category = Category.find_by_name("Wine") %>
                      <% children = Category.find(:all,:conditions=>['parent_id = ? and show_in_menu = ?',category.id,true] ) %>
                      <% children = children.sort{|x,y| y.id <=> x.id } %>
                      <%#- for child in category.children(:order=>"(lft1-rgt) asc") -%>
                      <%- for child in children -%>
                          <%# num_products = find_any_active_product_exists(child.products)  %>
                          <%#- if num_products > 0  -%>
                          <li><%#= link_to(child.name.html_safe(), nested_categories_url(%w(Beers Vodka Grappas Gin Amaro Vermouth).include?(child.name) ? wine_category : category, child)) %></li>
                          <li><%= link_to(child.name.html_safe(), nested_categories_url(category, child)) %></li>
                          <%#- end -%>
                      <%-  end -%>
                      <li style="font-weight:bold;">
                        <%= link_to("Special Offers", nested_categories_url(category.name == "Other Drinks" ? wine_category : category, "offer"), :type => "special_offer", :class => "red-text") %>
                      </li>
                    </ul>
                <%- end -%>
              </li>
          <% end %>
      <%- end -%>

      <!--<li class="<%#= " active" if active_uri == "/wine-club" %>"><%#= link_to "Wine Club", :action=>"wine_club", :controller=>"site/base" %></li>-->
      <li id="link-to-forum" class="<%= "active" if active_uri == "/wine-community" %>"><%= link_to "Community", wine_community_path %></li><!-- earlier it was if active_uri == "/forums" problem when mouser hover in this case -->
      </li>
      <li id="link-to-blog" class="last<%= " active" if active_uri == "/site/blog" %>"><%= link_to "Blog", site_blog_index_path %></li>

    </ul>
    <div style="clear:both;"></div>
    <%= content_tag(:div, @breadcrumbs.html_safe(), :class => "breadcrumbs") if @breadcrumbs %>
    <div class="breadcrumbs" >&nbsp;&nbsp;&nbsp;
      <strong>
        <% if logged_in? and !(current_user.orders.blank?)%>
            <% show_link = check_any_products_remains_for_review(current_user.orders) %>
            <% if show_link == true %>
                <span class="data"><%= link_to 'Why not review the products you bought?',:controller=>'orders',:action=>'show_order_details' %></span>
            <% end %>
        <% end %>
      </strong>
    </div>
    <%#= content_tag(:div, @setting.promotion, :id => "promotion") %>
    <div id="promotion"><%= render :partial => "/site/shared/promotion" %></div>
  </div>
  <div style="clear:both;"></div>
  <div id="mainContent">
    <%# if flash[:error] or flash[:notice] %>

    <!--   <div id="notice" style="font:bold italic large Palatino, serif;text-align:center;background:#F5F6CE" <%# if flash[:error] %> color="red"<%# else %> style="color:green;"<%# end %>><%#= flash[:error] || flash[:notice] %></div>
            <script type="text/javascript">
              setTimeout("new Effect.Fade('notice');", 12000)
            </script>
          <%# end %> -->
    <%#
          if !cookies[:display]
            cookies[:display] = {:value => 'true',
   :expires => 1.days.from_now }
    %>
    <!--  <script type="text/javascript">
      alert("We have just relaunched our website adding plenty of new functionality, we have done all tests and everything seems to be working fine, however, we ask you to be patience in case something does not work the way it is supposed to and please let us know so we can give it our immediate attention.");
      </script> -->
    <%#end%>


    <% if flash[:alert] %>
        <script type="text/javascript">
            document.observe('dom:loaded', function() {
                alert("<%= flash[:alert] %>");
            });
        </script>
    <% end %>
    <% @content_for_right = true %>
    <%= content_tag(:div, (yield :right), :id => "rightColumn") if @content_for_right -%>

    <div id="leftColumnContent" class="<%= "widescreen" unless @content_for_right %>">
      <%= yield %>
    </div>

  </div>
  <%= render :partial => 'site/shared/footer' %>



</div>


<div style="clear:both;"><div>
  <br /><br />
  <div style="font-size:10px;text-align:center;color:#000;">
    &copy; Copyright 2004-<%= Date.today.strftime("%Y") %> Italy Abroad Network Ltd, registered in England & Wales No. 4962185. All rights reserved.<br />
    Registered office: Oak Mill, Topcliffe Lane, Leeds, LS27 0HL. For more information see <a href="http://www.italyabroad.com/about-us">about us</a><br /><br /><br /><br />
  </div>

  <%- if RAILS_ENV == "production" -%>
      <script src="https://ssl.google-analytics.com/urchin.js" type="text/javascript">
      </script>
      <script type="text/javascript">
          _uacct = "UA-620606-1";
          urchinTracker();
      </script>
  <%- end -%>
  <script type="text/javascript">
      $(".chat_available").click(function (event) {
                  event.preventDefault();
                  var w = window.open("<%= site_chat_index_path -%>", "popupWindow", "width=490, height=540, scrollbars=yes");
                  var $w = $(w.document.body);
                  $w.html($('#modalText').html());
                  $w.find(".addimage").click(function(e) {
                      e.preventDefault();
                      console.log(w.opener.document);
                      $("#imagelist", w.opener.document).append("<li></li>");
                  });
                  return false;
              }
      );
  </script>
</body>
</html>

